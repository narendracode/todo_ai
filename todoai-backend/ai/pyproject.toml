[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[project]
name = "todoai-ai"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "langchain-anthropic>=0.3",
]

[tool.hatch.build.targets.wheel]
packages = ["src/todoai_ai"]
